package com.desarrollo.sem.service;



import javax.persistence.EntityManager;
import javax.persistence.NoResultException;
import javax.persistence.PersistenceContext;

import org.springframework.stereotype.Service;


import com.desarrollo.sem.model.UsuarioConductor;

//@Service
public class UsuarioConductorServiceBean /*implements UsuarioConductorService */{

/*   @PersistenceContext
  protected EntityManager em;
   

  public UsuarioConductor findMail(String mail) {
    try {
        return
        em.createQuery(
          "select e from UsuarioConductor e where e.mail = :mail", UsuarioConductor.class)
          .setParameter("mail", mail)
          .getSingleResult();
      } catch (NoResultException e) {
        return null;
      }
  } */
    
}
